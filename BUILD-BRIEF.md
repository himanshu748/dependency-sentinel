# Real-input product increment

Approved direction: local Python repository review with live OSV/PyPI evidence, an isolated worktree and downloadable reviewed patch. Repositories must be under the authorized root. Their tests execute code: the operator must trust them. A worktree is not a security sandbox.

Keep fixtures explicit. Decouple live public evidence from paid model use. The offline Strands tool loop uses deterministic selection, not live AI reasoning. No PR creation, source checkout modification, AWS deployment, or bank-funded usage. Preserve failed validation states and persist run history. First risk: regenerate real lockfiles and test the resolved environment, not the backend environment.
